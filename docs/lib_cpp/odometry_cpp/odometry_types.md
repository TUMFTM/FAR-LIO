# Odometry Types Library

This library defines all containers/structs for the [odometry_cpp](../odometry_cpp.md):

- **config** and **debug** objects of the single modules
- internal types (e.g. point-type, correspondence type) to be used as interface within
the library
- structs that define the compile-time parameters and modules of the final executables of
the library
