# Odometry Pipeline Library

TUM Autonomous Motorsport package for modular handling of odometry algorithms

## Design

The odometry pipelines combines the single modules to a full odometry algorithm.
It defines the order and parameters that the single modules are called and forms
the main interface for the whole library with e.g. the ROS2 node.
